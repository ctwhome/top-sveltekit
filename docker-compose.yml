version: '3.8'

services:
  powersync:
    image: powersync:latest
    environment:
      - NODE_ENV=${NODE_ENV}
      - DATABASE_URL=${DATABASE_URL}
    ports:
      - "3000:3000"
    volumes:
      - .:/app
    command: npm start

networks:
  default:
    external:
      name: powersync_network
